[project]
name = "mynd"
layer = "workspace"
description = "Persistent memory MCP server for AI coding agents"

[hooks.on_session_start]
max_tokens = 2000
recalls = [
  "tag:preferences",
  "tag:project:pipelines & tag:entrypoint",
  # Exact memory titles to auto-load at session start, e.g.:
  # "golang preferences",
  # "project/mynd",
]
